const BaseDao = require('{{data.relativePath}}common/base/BaseDao');
const bean = require('./{{data.moduleName}}.bean');

class Dao extends BaseDao {
}

module.exports = new Dao('{{data.tbName}}', bean);
